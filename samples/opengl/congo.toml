[package]
name = "opengl"
version = "0.1.0"
output = "executable"

# The program also links against the C libraries SDL2 and OpenGL, which the
# modules sdl and gl bind. Congo has no way to name a C library yet, so it
# compiles this package but cannot link it.
