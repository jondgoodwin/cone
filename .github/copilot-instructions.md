# Cone repository instructions

The canonical coding-agent instructions for this repository live in
[`CLAUDE.md`](../CLAUDE.md) at the repository root. Read that file.

It covers the repository layout, the compiler pipeline, code conventions, how
to build `conec`, and how to validate a change. This file is kept as a pointer
so the guidance has a single source and the two cannot drift apart.
