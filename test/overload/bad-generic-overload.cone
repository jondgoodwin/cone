// Negative fixture: a generic function may not also declare an overload name.
// Expected: Error 1054 "A generic function may not declare the overload name pick"

fn pickOne overload pick(n i32) i32:
  n

fn pickGeneric[T] overload pick(x T, y T) T:
  if x > y {x} else {y}
