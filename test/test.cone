// A test program in Cone that does nothing interesting

mut glowy = 34u32;
mut glo2 i32 = 7;

fn cone() u32 {
    mut unsy = 23;
    '\t';
    2.3e1;
    add(unsy); // Forward reference call + default parameter
    glowy = unsy;
    glo2 = glowy; // cast
    glowy; // Implicit return of global var's value
}

fn add(a i32, b = 1) i32 {
    a.`-`(b);
}
