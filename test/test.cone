// A test program in Cone that does nothing interesting

mut glowy = 34u32;
mut glo2 i32 = 7;

fn cone() u32 {
    mut unsy = 23;
    '\t';
    2.3e1;
    glowy = unsy;
    glo2 = glowy; // cast
    glowy; // Implicit return of global var's value
	bitwise(0x1a, 0x42);
    calc(4, 3); // Forward reference call
}

fn bitwise(a u32, b u32) u32 {
    (a & b | a ^ ~b).shl(1);
}

fn calc(a i32, b = 1) i32 {
    -a + (a+a)*b % b;
}
