// A core package with nothing in it, found ahead of the real one through
// '--path=test/cases/intrinsic/intrinsicpkgs'. The scenarios that use it are
// themselves modules named 'core', which is where '@intrinsic' is allowed, and
// they declare the registry's own names -- which the real core, as every
// module's prelude, would already hold.

mod core;
