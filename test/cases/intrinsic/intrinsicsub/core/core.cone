// A core package laid out as a folder, 'core/core.cone', which the compiler's
// sweep reads with the files beside it, found through
// '--path=test/cases/intrinsic/intrinsicsub' by intrinsic_nameres_submodule.
// Its submodule 'mem' declares an intrinsic: a submodule of core is core.

mod core;
