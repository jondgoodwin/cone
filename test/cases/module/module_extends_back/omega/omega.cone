// Imports every public name of alpha, which extends this module.

mod omega;

import alpha use *;    //~ ErrorImportLoop "module-extends-back.omega imports module-extends-back.alpha at"

pub fn four() i64 {
  4i64;
}

pub fn useAlpha() i64 {
  three() + four();
}
