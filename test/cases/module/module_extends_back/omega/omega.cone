// Imports every public name of alpha, which extends this module.

mod omega;

import alpha use *;    //~ ErrorImportLoop "module_extends_back.omega imports module_extends_back.alpha at"

pub fn four() i64 {
  4i64;
}

pub fn useAlpha() i64 {
  three() + four();
}
