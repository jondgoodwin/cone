// Extends base, which imports this module back with 'use *'.

mod rich extends base;    //~ ErrorImportLoop "module_extends_back.rich extends module_extends_back.base at"

pub fn two() i64 {
  one() * 2i64;
}
