// Extends base, which imports this module back with 'use *'.

mod rich extends base;    //~ ErrorImportLoop "module-extends-back.rich extends module-extends-back.base at"

pub fn two() i64 {
  one() * 2i64;
}
