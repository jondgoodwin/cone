// A submodule that happens to share its name with the stdio package. What it
// declares, the package has not got, so a call to it compiles only where the
// import reached this module.

mod stdio;

pub fn marker() i64 {
  42i64;
}
