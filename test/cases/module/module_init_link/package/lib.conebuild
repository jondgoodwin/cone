// Package lib, compiled on its own into a library object before the program is
// compiled. The runner builds it because cases.toml lists it under 'link', and
// links its object into the program.

build: debug
output: library

lib: {
    "lib.cone"
    import stdio: "../stdio.cone"
}
