// The retired 'use pub' spelling is refused the same way: the 'pub' is what is
// wrong here, wherever it is written.

mod retired use pub one;          //~ ErrorBadPub:13 "'pub' has nothing to say here"

pub fn one() i64 {
  1i64;
}
