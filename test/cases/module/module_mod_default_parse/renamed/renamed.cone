// A default folds each name under the module's own spelling. What an importer
// calls a name is the importer's to say, in a clause of its own.

mod renamed use one as uno;       //~ ErrorBadFold:17 "under the module's own spelling"

pub fn one() i64 {
  1i64;
}
