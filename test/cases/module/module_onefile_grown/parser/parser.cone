// A folder submodule, as in module_onefile. Its child ast has grown from
// ast.cone into ast/ast.cone, and nothing here says which it is.

mod parser;

// A sister, one file or a folder, is imported by name
import lexer;

pub fn parse(n i64) i64 {
  lexer.scan(n) + ast.depth();
}
