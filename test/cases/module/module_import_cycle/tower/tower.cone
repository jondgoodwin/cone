// Extends plinth, which imports this module. more is a name of plinth by its
// 'pub use', so it is a public name of tower too, once it has arrived.

mod tower extends plinth;    //~ ErrorImportLoop

pub fn height() i64 {
  more() * 10i64;
}
