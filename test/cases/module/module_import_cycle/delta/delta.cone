// alpha and beta's cycle in the other order. delta's folds start first and stop
// at its import to fold epsilon, which finds delta mid-fold, re-exports gamma's
// shared, and is complete when delta reads it. Nothing is lost in this order.

mod delta;

import epsilon use five, shared as common;

pub fn d() i64 {
  five() + common();
}
