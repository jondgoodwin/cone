// Folds Colors' variants in publicly and Shade's privately, which is what the
// other two files of this scenario test the reach of.

mod palette;

pub enum Colors {
  Red, Green, Blue;
}
use pub Colors;

pub enum Shade {
  Light, Dark;
}
use Shade;
