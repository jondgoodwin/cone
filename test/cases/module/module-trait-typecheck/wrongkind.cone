// step is required to be a function and is a global here; scale is required to
// be a global and is a function here.

mod wrongkind is Runner;       //~ ErrorModTraitMismatch:18 "requires step to be a function"
                               //~^ ErrorModTraitMismatch:18 "requires scale to be a global"

pub mut step = 1i64;

pub fn scale() i64 {
  2i64;
}
