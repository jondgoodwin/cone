// The program, compiled against the include file q's compile generated (the
// golden file under include\), and linked with q's object and conestd.

build: debug
output: executable

import stdio: "stdio.cone"
import q: "include/q.cone"

prog: {
    "main.cone"
    import q: "include/q.cone"
    import stdio: "stdio.cone"
}
