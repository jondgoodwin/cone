// A file of module_header_reject that is not its designated file: no 'mod'
// line, and that is accepted. Its import, the first thing it says, is not.

import stdio;      //~ ErrorImportLate "designated file"

fn second() i64 {
  2i64;
}
