// A file of module_header_reject that is not its designated file: no 'mod'
// line, and that is accepted. Its import comes after a declaration, and that is
// not.

fn second() i64 {
  2i64;
}

import stdio;      //~ ErrorImportLate "right after the 'mod' line"
