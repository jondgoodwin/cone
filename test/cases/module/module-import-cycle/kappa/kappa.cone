// Imports lambda and relay, each of which imports this module back, and then
// re-exports gamma's more: a fold that has not run when either reads this module
// in the first pass.

mod kappa;

import lambda;
import relay;
import gamma pub use more;

pub fn k() i64 {
  4i64;
}
