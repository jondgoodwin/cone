// The names the other modules re-export: shared, more, Tone, Config. gamma is in
// no cycle.

mod gamma;

pub fn shared() i64 {
  1i64;
}

pub fn more() i64 {
  2i64;
}

pub enum Tone {
  Low, High;
}

pub struct Config {
  pub speed i64;
  pub fn twice(self) i64 {
    self.speed * 2i64;
  }
}
