// Two routes to gamma's more: privately from gamma, and publicly from kappa,
// which is mid-fold when this reads it (kappa -> relay -> kappa). The private
// route binds more in the first pass; the public one arrives in the next, and
// the one binding becomes public, as it would have in a single pass.

mod relay;

import gamma use more;
import kappa pub use *;
