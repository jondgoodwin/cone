// Closes the cycle. Each module's names reach it back by the other two, as the
// same declarations: one binding each, whichever route arrives first.

mod tri3;

import tri1 pub use *;

pub fn three() i64 {
  3i64;
}
