// tri1 -> tri2 -> tri3 -> tri1. tri1's folds start first, so tri3 reads it
// mid-fold, before tri1 has taken tri2's names or re-exported gamma's shared.

mod tri1;

import tri2 pub use *;
import gamma pub use shared;

pub fn one() i64 {
  1i64;
}
