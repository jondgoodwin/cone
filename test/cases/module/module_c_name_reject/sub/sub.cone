mod sub;

extern {
  fn @c abs(n i64) i64;              //~ ErrorCNameConflict:3 "The C name abs is declared differently at"
  fn @c(system) labs(n i64) i64;     //~ ErrorCNameConflict:3 "The C name labs is declared differently at"
}

pub fn @c cone_twice(n i64) i64 {    //~ ErrorCNameDefTwice:5 "The C name cone_twice is defined already, at"
  n + n;
}
