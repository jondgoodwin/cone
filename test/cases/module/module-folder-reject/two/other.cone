// The other half of the collision in one/dup.cone. Parsed second, because the
// sweep reads a folder's subfolders in name order, so this is the declaration
// reported as the duplicate.

fn shared() i64 {                //~ ErrorDupName "already defined"
  2i64;
}
