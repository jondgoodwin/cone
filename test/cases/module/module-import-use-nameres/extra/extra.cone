// A second source, which relay folds privately. It shares one name with lib,
// which is what a module folding both has to settle.

mod extra;

pub fn more() i64 {
  4i64;
}

pub fn two() i64 {
  5i64;
}
