// Re-exports the folds of one import with 'pub use', and folds another
// privately. Neither import is 'pub import', so neither module's own name is a
// public name of relay.

mod relay;

import lib pub use one, two as deux;
import extra use *;

pub fn sum() i64 {
  one() + deux() + more();
}
