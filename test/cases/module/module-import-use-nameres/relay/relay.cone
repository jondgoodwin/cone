// Re-exports the folds of one import with 'use pub', and folds another
// privately. Neither import is 'pub import', so neither module's own name is a
// public name of relay.

mod relay;

import lib use pub one, two as deux;
import extra use *;

pub fn sum() i64 {
  one() + deux() + more();
}
