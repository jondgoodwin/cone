// Stands for stdio's include file: the declarations a program compiles against,
// with the definitions supplied elsewhere -- here, by conestd's C functions.

mod stdio;

pub extern {
  fn @c printStr(str &[]u8);
  fn @c printInt(a i64);
  fn @c printFloat(a f64);
}
