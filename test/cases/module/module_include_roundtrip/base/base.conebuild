// Package base, compiled on its own into a library object, which writes its
// include file. Package t compiles against that include file, as the program
// does.

build: debug
output: library

base: {
    "base.cone"
}
