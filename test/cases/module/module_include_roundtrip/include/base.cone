// Generated by conec: the include file of package base, from base.cone.
// A program importing base compiles against this file in place of that source.
// Do not edit it: change base's source and compile base again.

// Package base: what package t extends. Every declaration here becomes a name
// of t as well, still base's own, so a program reaches base's function and
// type through t and links against base's object.

mod base;

pub extern fn origin() i64;

pub struct Pair {
  pub x i64;
  pub y i64;

  pub extern fn sum(self) i64;
}
