// 'extends' names a module by one name, the one it is reached by: a sister, or a
// module this one imports. A module further away is imported first, and then
// named here.

mod dotted extends pair.inner;            //~ ErrorModExtends:24 "by one name"
