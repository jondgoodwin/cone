// The description names this child 'kid'; its first file may declare it, and
// only by that name.

pub mod child;  //~ ErrorBuildModName "lists this file in module 'kid', and its 'mod' line names 'child'"

pub fn four() i64 {
  4i64;
}
