// Package a, which imports b and hands b's type across its public functions.

mod a;

import b;

pub fn start(n i64) b.Counter {
  b.Counter[n, 3i64];
}

pub fn advance(c b.Counter) b.Counter {
  b.Counter[c.next(), c.step];
}
