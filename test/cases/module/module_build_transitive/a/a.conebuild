// Package a, compiled on its own into a library object, against b's include
// file: its module's import line and the package line both name it, as Congo
// writes them.

build: debug
output: library

import b: "../include/b.cone"

a: {
    "a.cone"
    import b: "../include/b.cone"
}
