// Package b's include file, written by hand: b's methods are declared 'extern'
// and defined in b's object.

mod b;

pub struct Counter {
  pub count i64;
  pub step i64;

  pub extern fn next(self) i64;

  pub extern fn scaled(self, k i64) i64;
}
