// A sister of facade, importing a name facade folded without 'pub'. A fold is
// private to the module that made it, so there is nothing public to import.

mod viewer;

import facade use innerValue;   //~ ErrorNotPublic:19 "innerValue is private to facade, so it does not fold"
