// No 'mod' declaration, so this file is one of its folder's module's files, as
// every file of a module folder was before a file could be a module of its own.

fn helperAdd(a i64, b i64) i64 {
  a + b;
}
