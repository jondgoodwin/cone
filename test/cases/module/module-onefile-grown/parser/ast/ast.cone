// module-onefile's parser/ast.cone, grown into a folder: the designated file of
// parser/ast/, drawing the submodule that one file drew, under the same name.

pub mod ast;

pub fn depth() i64 {
  3i64;
}
