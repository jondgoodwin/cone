// A hand-written include file: an import line names it, and it has no banner,
// so it may not hold a module block.

mod hand;

mod inner {                  //~ ErrorUnbuiltKind "Only a generated include file writes a module block"
  pub extern fn f() i64;
}
