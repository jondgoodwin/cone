// Generated by conec: the include file of package prog, from main.cone.
// The banner alone does not make a file an include file: this one is the
// program's own source, so its block is refused as source's is.

mod prog;

import hand;
import gen;

mod inner {                  //~ ErrorUnbuiltKind "Only a generated include file writes a module block"
  fn helper() i64 {
    1i64;
  }
}

fn main() i32 {
  0i32;
}
