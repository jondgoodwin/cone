mod u32;                    //~ ErrorDupName:1 "u32 is already the name of a built-in type"
                            //~^ ErrorDupName:1 "u32 is already the name of a built-in type"

// A one-file submodule named after a built-in number type.

pub fn f() {
}
