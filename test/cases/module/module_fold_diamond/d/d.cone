// The bottom of the diamond: x arrives through a and through b, and it is c's
// x both times, so the second is the binding x has already.

mod d;

import a use *;
import b use *;

pub fn y() i64 {
  x() + 41i64;
}
