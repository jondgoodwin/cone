// A wildcard import of relay. What it receives is what relay re-exported: the
// module bigint and the type its default folded into relay.

mod gallery;

import relay use *;

fn make(n i64) BigInt {
  BigInt[n];
}

pub fn show(n i64) i64 {
  imm b = make(n);
  b.doubled() + bigint.helper(n);
}
