// An import writing a clause of its own, which REPLACES the default: inches is
// folded, and metres and feet are not. module_mod_default_nameres pins metres
// refused bare after a clause like this one.

mod picky;

import units use inches;

pub fn measure(n i64) i64 {
  inches(n) + units.metres(n);
}
