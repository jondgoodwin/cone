// A program compiled against q's include file, whose 'extern' globals fold
// through types it cannot see. q's include file imports other's, which the
// package line for other answers.

import other: "include/other.cone"
import q: "include/q.cone"

p: {
    "src/p.cone"
    import q: "include/q.cone"
}
