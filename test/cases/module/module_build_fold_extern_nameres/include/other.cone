// other's include file: one public type and one private one, which an include
// file declares where an inline body of the package reaches it.

mod other;

pub struct Shown {
  pub width i64;
}

struct Secret {
  pub depth i64;
}
