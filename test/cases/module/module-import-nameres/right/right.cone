// The sister left imports. A sister sees a sister's public surface and nothing
// nested privately inside her, which is the boundary holding at one level rather
// than being one level deep.

mod right;

pub fn shown(n i64) i64 {
  n;
}

fn hidden(n i64) i64 {
  n + 1i64;
}
