// Imported by module_collision.cone, and never compiled on its own, so it is
// listed in cases.toml as a support module.
//
// It declares 'y_z' so that its prefix joined to that name spells the same
// symbol as modulex_y.cone's prefix joined to 'z'.

mod modulex;

pub fn y_z() i64 {
  2i64;
}
