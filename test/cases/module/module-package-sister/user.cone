// Imports stdio by name. The registry its parent is holds a sister of that name,
// so the lookup ends there: nothing is located, and the packages folder is not
// searched.

mod user;

import stdio;

pub fn value() i64 {
  stdio.marker();
}
