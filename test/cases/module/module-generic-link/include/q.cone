// Package q's include file: what q's object defines, declared 'extern'. That
// q uses g[i64] is nowhere in it: q's object carries its own copy of the
// instance, which merges with the program's at link.

mod q;

pub extern fn addTwice(n i64) i64;

pub extern fn total() i64;
