// A second sister, wildcard-imported by engine so that its public names become
// names of engine. A fold across the tree is the same fold an import across a
// package boundary makes: an alias per name, each with a visibility of its own.

mod rims;

pub fn size() i64 {
  15i64;
}
