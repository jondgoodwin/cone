// A 'mod' line's default fold is what a bare import of the module folds, and a
// generic module has nothing of its own to fold

pub mod defaulted[T] use push;    //~ ErrorGenModBare:22

pub fn push(x T) {
}
