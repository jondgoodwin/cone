// A generic module drawn from a folder, holding a one-file submodule. The
// submodule would be instantiated with it, as a generic type's methods are,
// and an instance is not built with submodules yet

pub mod holder[T];

pub mut kept i64 = 0i64;
