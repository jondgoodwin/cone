// Package b, compiled on its own into a library object, with its submodule.

build: debug
output: library

b: {
    "b.cone"
    import stdio: "../stdio.cone"
    sub: {
        "sub.cone"
        import stdio: "../stdio.cone"
    }
}
