// Package c, compiled on its own into a library object, against b's include
// file, which imports stdio: a package line for each.

build: debug
output: library

import stdio: "../stdio.cone"
import b: "../include/b.cone"

c: {
    "c.cone"
    import stdio: "../stdio.cone"
    import b: "../include/b.cone"
}
