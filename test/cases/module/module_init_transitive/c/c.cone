// Package c, which imports b too: the other side of the diamond.

mod c;

import stdio use *;
import b;

fn @initpure init() {
  printStr("c init\n");
}

fn final() {
  printStr("c final\n");
}

pub fn twiceB() i64 {
  b.get() * 2i64;
}
