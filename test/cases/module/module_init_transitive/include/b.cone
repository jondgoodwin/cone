// Generated by conec: the include file of package b, from b.cone.
// A program importing b compiles against this file in place of that source.
// Do not edit it: change b's source and compile b again.

// Package b, at the bottom of a diamond: the program imports a and c, and each
// of them imports b. The program never imports b, so b's 'init' and 'final' are
// reached only through a's and c's include files, which import b's. Its 'init'
// reads its submodule's global, so the submodule's 'init' must run first.

mod b;

import stdio use *;

// b's submodule: b contains it, so it depends on nothing b has, and b depends
// on it. Its 'init' runs before b's, and its 'final' after b's.

mod sub {
import stdio use *;

extern fn @initpure init();

extern fn final();
}

pub extern imm value i64;

extern fn @initpure init();

extern fn final();

pub extern fn get() i64;
