// The control for a wildcard: what it did not carry, because quiet never had
// it, keeps the message it always had, bare here and by a path from outside.

mod solitary;

import quiet use pub *;

fn g() i64 {
  shared();                 //~ ErrorUnkName:3 "The name shared does not refer to a declared name"
}
