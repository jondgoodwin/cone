// Imports fox and foxtail, each of which imports this module back, and then
// re-exports delta's shared. Its folds start first, so both read it before that
// re-export has run, and it reaches them in the next pass.

mod echo;

import fox;
import foxtail;
import delta pub use shared;
