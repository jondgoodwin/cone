// Imports beta, which imports this module back. alpha's folds start first, so
// beta reads it mid-fold, before the re-export below.

mod alpha;

import beta;
import gamma pub use shared;

pub fn own() i64 {
  3i64;
}
