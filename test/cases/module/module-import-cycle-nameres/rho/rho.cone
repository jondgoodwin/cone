// The same shape with a wildcard in place of the first listing: what the
// wildcard carries round the cycle was never written here, so it is the binding
// gamma's listing made, and nothing is refused.

mod rho;

import sigma use *;
import gamma pub use shared;

pub fn r() i64 {
  shared();
}
