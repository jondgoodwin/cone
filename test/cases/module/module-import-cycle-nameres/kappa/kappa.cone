// Imports lambda and nu, each of which imports this module back, and then
// re-exports more and extra and folds hidden privately: folds that have not run
// when either reads this module in the first pass.

mod kappa;

import lambda;
import nu;
import gamma pub use more, extra;
import hush use hidden;

pub fn k() i64 {
  4i64;
}
