// Imports lambda, which imports this module back, and then re-exports gamma's
// more: a fold that has not run when lambda reads this module.

mod kappa;

import lambda;
import gamma use pub more;

pub fn k() i64 {
  4i64;
}
