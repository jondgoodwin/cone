// A module two of its sisters import, each differently: draw folds every name
// but one, and gallery folds two with a block and re-exports all it binds.

mod units;

pub fn metres(n i64) i64 {
  n * 1000i64;
}

pub fn feet(n i64) i64 {
  n * 305i64;
}

pub fn inches(n i64) i64 {
  n * 25i64;
}
