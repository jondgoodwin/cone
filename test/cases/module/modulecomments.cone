// Imported by module_parse_nomod.cone, and never compiled on its own, so it is
// listed in cases.toml as a support module.
//
// A module's first file holding nothing but comments. It has no 'mod' line
// and no statement to report that against, so it is reported at the end of
// the file, which is this line.  //~ ErrorNoModDcl "begin it with 'mod modulecomments;'"
