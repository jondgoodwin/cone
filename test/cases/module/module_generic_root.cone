// A program whose root module is generic. Nothing names an instance of the
// program's root, so nothing of it would ever be compiled: an executable's
// root is refused as a generic module, which a submodule, an imported module
// or a library's root may be.

mod module_generic_root[T];             //~ ErrorGenModRoot:1

fn main() i32 {
  0i32;
}
