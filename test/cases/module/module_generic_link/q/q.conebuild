// Package q, compiled on its own into a library object before the program is
// compiled. It imports package g through g's include file, and instantiates g
// at i64 itself.

build: debug
output: library

q: {
    "q.cone"
    import g: "../include/g.cone"
}
