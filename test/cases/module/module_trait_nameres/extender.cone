// 'extends' reuses a concrete module. A module conforms to a module trait with
// 'is', and that is what the refusal says to write.

mod extender extends Runner;   //~ ErrorModExtends:22 "with 'is'"
