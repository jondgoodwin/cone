// Runner requires a function and a global and gives neither a default, and this
// module declares neither: each is reported at the 'is', naming the member.

mod missing is Runner;         //~ ErrorModTraitMissing:16 "function step"
                               //~^ ErrorModTraitMissing:16 "global scale"
