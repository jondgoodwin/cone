// A submodule's submodule. 'pub' opens it to geometry's neighbours, so the root
// may name a path through geometry to it, and fold what that path reaches.

pub mod deep;

pub fn depth() i64 {
  3i64;
}
