// A third module, a sister of facade. What facade re-exported with 'pub use' is a
// public name of facade, so an import's clause folds it here like any other.

mod reader;

import facade use innerValue;

pub fn readInner() i64 {
  innerValue() + 1i64;
}
