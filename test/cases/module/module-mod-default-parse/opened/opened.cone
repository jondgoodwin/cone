// Each import decides how visible its folds are, 'pub import' among them, so a
// default has no visibility of its own to declare.

mod opened pub use one;           //~ ErrorBadPub:12 "'pub' has nothing to say here"

pub fn one() i64 {
  1i64;
}
