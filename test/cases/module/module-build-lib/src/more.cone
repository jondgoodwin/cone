// A second file of module q, listed after q.cone: it declares nothing about the
// module, and its declarations are q's like the first file's.

pub struct Acc {
  pub total i64;

  pub fn put(self &mut, n i64) {
    self.total = self.total + addOne(n);
  }
}
