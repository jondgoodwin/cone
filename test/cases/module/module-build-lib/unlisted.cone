// Beside the build description and listed nowhere in it, so not compiled: a
// described build sweeps nothing. Nothing is spelled after this file's name or
// after its function's.

pub fn unlistedFn() i64 {
  0i64;
}
