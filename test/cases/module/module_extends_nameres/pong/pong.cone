// The other half of ping's loop. The walk that orders the modules reaches ping
// first, the program holding it first, so this 'extends' is the edge that
// closes the loop, and it is reported and cut here.

mod pong extends ping;                    //~ ErrorImportLoop "extends module_extends_nameres.ping at"
