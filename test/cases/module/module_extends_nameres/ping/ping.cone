// Two sisters each extending the other. Each would be adding to the other, and
// neither has a surface to start from. That is an import loop like any other
// (ErrorImportLoop): reported once, at the 'extends' that closes it, in pong,
// and cut there, so nothing more is said here.

mod ping extends pong;
