/* A one-file module whose 'mod' declaration follows a block comment. What the
   sweep reads is the first statement, so white space and comments in front of it
   do not hide it. */

mod tokens;

pub fn weight(n i64) i64 {
  n * 10i64;
}
