// The control for a clause: a name quiet has not got, and no cycle involved,
// keeps the message it always had.

mod solo;

import quiet use shared;    //~ ErrorNoMbr:18 "quiet has no name shared to fold in."
