// The module module-extends-import extends: a module reached by an import that
// names its file, which is what reaches a module outside the tree today.

mod moduleextbase;

pub fn twice(n i64) i64 {
  n * 2i64;
}

pub mut total = 0i64;
