// Module q's child, named 'inner' by the build description. Its 'pub' is what
// opens it to q's neighbours, as a submodule's own declaration always does.

pub mod inner;

pub fn triple(n i64) i64 {
  n * 3i64;
}
