// An ordinary submodule, here only so that the import in the scenario's
// designated file has one to name. It is reached by writing 'sub.subFn', never by
// importing it.

mod sub;

pub fn subFn(n i64) i64 {
  n;
}
