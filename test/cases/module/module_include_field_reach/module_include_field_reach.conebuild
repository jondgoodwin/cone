// The program, compiled against the include file h's compile generated (the
// golden file under include\), and linked with h's object and conestd.

build: debug
output: executable

import stdio: "stdio.cone"
import h: "include/h.cone"

prog: {
    "main.cone"
    import h: "include/h.cone"
    import stdio: "stdio.cone"
}
