mod h;

struct Inner {
  pub v i64;
  pub fn get(self) i64 {
    v;
  }
}

pub struct Outer {
  pub inner Inner;
}

pub fn make(v i64) Outer {
  Outer[Inner[v]];
}

pub fn val(o Outer) i64 inline {
  o.inner.get();
}
