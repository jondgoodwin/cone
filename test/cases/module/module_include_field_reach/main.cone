// The program: compiled against the include file h's compile generated, and
// linked with h's object. It calls Inner's method through Outer's field twice:
// in h's inline body, expanded here, and in its own code.

mod prog;

import stdio use *;
import h;

fn main() i32 {
  printInt(h.val(h.make(9i64)));
  printStr("\n");
  printInt(h.make(4i64).inner.get());
  printStr("\n");
  0i32;
}
