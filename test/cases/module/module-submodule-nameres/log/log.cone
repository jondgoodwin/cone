// A sister of store: both are submodules of this scenario's root module. It is
// public in every way it can be, and store still cannot name it -- what stops the
// reach is that nothing binds 'log' in store's namespace, not what log shows.

pub mod log;

pub fn write(n i64) i64 {
  n;
}
