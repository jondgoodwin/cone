mod sister;

import Option;              //~ ErrorDupName:1 "Option is already a name of this module, and core"

// Importing the sister named Option binds the name here, where core's Option
// has it already: reported at the import.

pub fn g() {
}
