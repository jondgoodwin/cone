// The include file p's import line names as 'a'. Its import of b is answered
// by the package line for b; nothing answers its import of c.

mod a;

import b;
import c;   //~ ErrorBuildImport "no package line for c, which 'import c' in module a names"

pub extern fn five() i64;
