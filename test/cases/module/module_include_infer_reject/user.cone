// Package user. Written with its type, 'pub mut first coll.Stack = ...', the
// global compiles: the include file copies the written type.

mod user;

import coll;

pub mut first = coll.Stack[1i64];   //~ ErrorIncCheck "cannot spell the type inferred from the value"
