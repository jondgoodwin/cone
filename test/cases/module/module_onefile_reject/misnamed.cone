// A one-file module is named for its file, as a module folder is named for its
// folder: the declaration may restate the name, and may not change it.

mod other;         //~ ErrorModName "named for its file, 'misnamed.cone'"

pub fn misnamedFn() i64 {
  2i64;
}
