// A one-file module declares itself once, in its first statement. A second
// declaration has nothing left to declare.

mod twice;

pub fn twiceFn() i64 {
  3i64;
}

mod twice;         //~ ErrorModDcl "first statement"
