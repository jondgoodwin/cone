// Imported by module-collision.cone, and never compiled on its own, so it is
// listed in cases.toml as a support module.
//
// Its name ends in 'y' so that its prefix joined to 'z' spells the same symbol
// as modulex.cone's prefix joined to 'y_z'.

pub fn z() i64 {
  1i64;
}
