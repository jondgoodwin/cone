// A global declared without a value, in a module that declares no 'init' to
// assign it one. The global with a value needs none.

mod noinit;

mut lonely i64;               //~ ErrorGlobalUninit:5 "declares no init"
pub imm fine i64 = 3i64;
