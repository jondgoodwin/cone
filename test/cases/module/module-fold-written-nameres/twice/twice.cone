// The same name listed twice in one clause. It was one binding, as the same
// declaration reached twice; written twice, it is refused at the second.

mod twice;

import c use x, x;                //~ ErrorDupName:17 "x is already a name of this module, for the same thing"

pub fn t() i64 {
  x();
}
