// The same declaration listed through two imports: c's own x, and a's
// re-export of it. Two written ways of bringing in one thing.

mod both;

import c use x;
import a use x;                   //~ ErrorDupName:14 "x is already a name of this module, for the same thing"

pub fn b() i64 {
  x();
}
