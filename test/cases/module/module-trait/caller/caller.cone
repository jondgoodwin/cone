// A bare import of layered folds what layered's 'mod' line names as its default,
// 'run': a default of Runner that plain took, and layered took from plain.

mod caller;

import layered;

pub fn go(n i64) i64 {
  run(n);
}
