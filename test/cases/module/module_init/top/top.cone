// Imports middle, and declares a 'final' and nothing else: the stitched final
// calls it, and the stitched init has nothing of top's to call.

mod top;

import stdio use *;
import middle;

fn final() {
  printStr("final top\n");
}
