// A second file of module prog. It imports nothing of its own: the module's
// imports are the module's, whichever of its files wrote them.

fn show(label &[]u8, n i64) {
  printStr(label);
  printStr(" = ");
  printInt(n);
  printStr("\n");
}

fn helper(n i64) i64 {
  n + 10i64;
}
