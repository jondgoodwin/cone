// A C-named module declaring the global tally defines, and defining the one
// tally declares.

mod @c peek;

pub extern {
  mut Hits i64;
}

pub imm Greeting [5; u8] = "hello";

pub fn Bump() i64 {
  Hits = Hits + 1i64;
  Hits;
}

pub fn Last() i64 {
  i64[Greeting[4]];
}
