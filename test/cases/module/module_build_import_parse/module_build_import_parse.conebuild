// In a described build the compiler never searches: an import is found where
// the description's import line for its name says, and one the description
// provides nothing for is refused -- even 'stdio', which a compile pointed at a
// source file finds in the packages folder.

p: {
    "src/p.cone"
    import dep: "dep.cone"
}
