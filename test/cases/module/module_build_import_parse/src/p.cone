// The one file of module p.

mod p;

import dep;     // provided: the description's import line says where
import stdio;   //~ ErrorBuildImport "provides nothing for 'import stdio' in module p"
import "dep";   //~ ErrorBuildImport "a path is not searched for"

fn main() {
  dep.five();
}
