// Two folders down, which is what "at any depth" means. It names a declaration
// from the subfolder above it and one from a sibling of the designated file,
// because all of them are one module's names.

fn deepestOf(n i64) i64 {
  nestedScale(n) + siblingAdd(0i64);
}
