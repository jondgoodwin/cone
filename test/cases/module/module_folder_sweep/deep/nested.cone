// A file of a subfolder. The subfolder holds no designated file of its own, so
// it is organisational: it groups files without minting a namespace for them,
// and its files belong to the enclosing module.

fn nestedScale(n i64) i64 {
  n * 10i64;
}
