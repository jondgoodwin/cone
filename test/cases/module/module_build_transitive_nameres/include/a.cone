// a's include file. Its import of b is private, as any module's import is
// unless it says 'pub': it binds b in a's namespace for a's own declarations.

mod a;

import b;

pub extern fn make() b.Counter;
