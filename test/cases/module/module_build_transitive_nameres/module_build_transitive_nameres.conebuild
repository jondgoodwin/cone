// The package lines put b in the compile, since a's include file imports it.
// That makes b's include file part of what the program is compiled against, and
// nothing more: the program imports a alone.

import a: "include/a.cone"
import b: "include/b.cone"

p: {
    "src/p.cone"
    import a: "include/a.cone"
}
