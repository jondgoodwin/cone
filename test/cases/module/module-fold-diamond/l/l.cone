// x listed from a, then brought by a wildcard of b. The wildcard never wrote
// the name, so the two are one binding: nothing here names x twice.

mod l;

import a use x;
import b use *;

pub fn lx() i64 {
  x() + 10i64;
}
