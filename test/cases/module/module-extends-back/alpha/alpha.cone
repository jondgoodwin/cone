// Extends omega, which imports this module back with 'use *'. Its folds run
// before omega's, since the program holds alpha first.

mod alpha extends omega;

pub fn three() i64 {
  four() - 1i64;
}
