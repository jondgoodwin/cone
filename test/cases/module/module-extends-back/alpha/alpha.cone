// Extends omega, which imports this module back with 'use *'. The program holds
// alpha first, so the walk that orders the modules starts here, and the loop
// closes at omega's import.

mod alpha extends omega;

pub fn three() i64 {
  four() - 1i64;
}
