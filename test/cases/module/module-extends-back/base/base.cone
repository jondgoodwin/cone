// Imports every public name of rich, which extends this module. Its folds run
// before rich's, since the program holds base first.

mod base;

import rich use *;

pub fn one() i64 {
  1i64;
}

pub fn useRich() i64 {
  two() + one();
}
