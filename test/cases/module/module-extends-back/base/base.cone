// Imports every public name of rich, which extends this module. The program
// holds base first, so the walk that orders the modules starts here, and the
// loop closes at rich's 'extends'.

mod base;

import rich use *;

pub fn one() i64 {
  1i64;
}

pub fn useRich() i64 {
  two() + one();
}
