// 'pub import' makes every binding the import makes public, and a bare import's
// bindings include what the default folds: bigint and BigInt are both public
// names of relay.

mod relay;

pub import bigint;
