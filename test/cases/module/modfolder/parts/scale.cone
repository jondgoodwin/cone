// A file of an organisational subfolder of modfolder, reaching declarations
// from both of the module's other files. An importer names it through the
// module, never through the subfolder, because the subfolder is not a namespace.

pub fn partsOf(n i64) i64 {
  helperScale(n) + folderAdd(n);
}
