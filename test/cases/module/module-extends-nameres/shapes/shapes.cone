// The base solids extends: public names, and one private.

mod shapes;

pub fn area(w i64, h i64) i64 {
  w * h;
}

pub fn perimeter(w i64, h i64) i64 {
  2i64 * (w + h);
}

// Private. 'extends' brings in what shapes makes public and nothing more
fn secret() i64 {
  7i64;
}
