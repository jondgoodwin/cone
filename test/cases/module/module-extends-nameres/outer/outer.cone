// A module extending one of its OWN submodules. A submodule is a part of the
// module that contains it, not something beside it to add to.

mod outer extends deep;                   //~ ErrorModExtends "inside this one"
