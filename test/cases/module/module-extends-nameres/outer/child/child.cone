// A module extending its PARENT. Containment runs one way, as it does for an
// import: a child may not name the module that contains it.

mod child extends outer;                  //~ ErrorModReach "may not extend the module that contains it"
