// A module extending one OUT OF ITS REACH. shapes is a sister of outer, not of
// this module: the registry is outer's namespace and no ancestor's, and nothing
// here imports shapes. 'extends' reaches what an import would, and loads nothing.

mod deep extends shapes;                  //~ ErrorUnkName "no module this module can reach"
