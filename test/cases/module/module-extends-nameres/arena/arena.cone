// 'extends' naming a trait. The module design brief spells a module conforming
// to a module trait this way -- 'mod arena extends Region' -- which is a
// different reading from reuse, and nothing is behind it. Refused, rather than
// taken as reuse of something that has no names of its own to reuse.

mod arena extends Region;                 //~ ErrorModExtends "not built"
