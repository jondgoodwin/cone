// Two sisters each extending the other. Each would be adding to the other, and
// neither has a surface to start from. Reported once, at the first of the two
// the program holds, and the chain is cut there.

mod ping extends pong;                    //~ ErrorModExtends "may not come back to where it started"
