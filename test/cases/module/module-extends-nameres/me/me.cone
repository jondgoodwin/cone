// A module's own name is a name of its own namespace, so 'extends' finds it --
// and a module has nothing to add to itself.

mod me extends me;                        //~ ErrorModExtends "cannot extend itself"
