// The file q's import line names as 'dep'. The import's name is the module's,
// so a 'mod' line naming another is refused.

mod other;  //~ ErrorBuildModName "imports this file as 'dep', and its 'mod' line names 'other'"

pub fn five() i64 {
  5i64;
}
