// Listed in module q, and declaring another module: reported as a file in the
// wrong module, although it is not q's first file.

mod elsewhere;  //~ ErrorBuildModName "lists this file in module 'q', and its 'mod' line names 'elsewhere'"

pub fn two() i64 {
  2i64;
}
