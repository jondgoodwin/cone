// Listed in module q, and restating q's name after q.cone already declared it:
// the name is right, and a module still declares itself once.

mod q;  //~ ErrorModDcl

pub fn three() i64 {
  3i64;
}
