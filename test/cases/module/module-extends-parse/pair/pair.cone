// A module extends ONE module, as a type enriches one concrete base. Reusing two
// raises what a name both have means, and nothing has asked that.

mod pair extends dotted, bare;            //~ ErrorExtends:24 "A module extends one module"
