// Module ex's child. Its public function is exported as the root's are; its
// private one, which nothing expanded names, is not.

pub mod inner;

pub fn innerPub() i64 {
  innerPriv();
}

fn innerPriv() i64 {
  3i64;
}
