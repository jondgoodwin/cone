// A range that includes its end, '...', must say where it ends: 'a..' runs to
// the end, and 'a...' names nothing to include. Parse stage.

mod collection_parse_range;

fn tail(s &[]i32) {
  imm r = &s[1...];   //~ ErrorBadIndex:18 "must say where it ends"
}
