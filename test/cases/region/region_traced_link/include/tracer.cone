// Generated by conec: the include file of package tracer, from tracer.cone.
// A program importing tracer compiles against this file in place of that source.
// Do not edit it: change tracer's source and compile tracer again.

// Package tracer's source: a traced region ref, compiled on its own into a
// library object. Every method of 'tr' is private and none is inline, as a
// collector's are: the program's object calls 'alloc' where it allocates, and
// 'mark' from the traces the compiler generates there, so the library exports
// both.

mod tracer;

import stdio use *;

pub struct tr is RegionRef, Traced {
  pub id i64;
  pub ty *TypeRecord;

  extern fn alloc(size usize, ty *TypeRecord) *u8;
  extern fn mark(self &uni tr, perm u32, mode u32);
}
