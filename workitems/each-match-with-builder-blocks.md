
### Each

Each and Range Iterator
- Create range as a type able to generate Option-based iterator
- Modify ‘each’ to work with iterator
- Fix ‘each’ parsing sugar for ranges

Closures/Iterators/Each
- Parse
- Semantic lowering
- Collection based iterator
- Virtual references (traits)
- Fn traits

Each: 
- closure-based iterators
- ‘Each’ for array references
- Documentation
- ‘Each’ for iterators
- ‘Each’ for collections
### Match

Match: partial and first class matches
- Handle more comparison operators <=, >=,
- Handle partial match
- Handle ‘using’

### With and Builder Blocks

With blocks and operator

Builder blocks
- Implement Stream struct and ‘out’ and ‘err’ as instances
- Support ‘<<’ {} or builder block
- Replace ‘:’ this logic with .[]?
- Interpolated strings?