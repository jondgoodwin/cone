# Commits that git blame should look through: mechanical changes that carry no
# authorship of their own. GitHub reads this file; locally, run
#     git config blame.ignoreRevsFile .git-blame-ignore-revs

# The layout move, moves only (src/c-compiler -> compiler/c, design/ ->
# doc/design and compiler/c/doc, coneref -> doc/reference). Blame follows whole
# file renames anyway; listed so nothing is ever attributed to the move.
c88cb1fffdc54d031254f1e6066d6986c4edf6a1
