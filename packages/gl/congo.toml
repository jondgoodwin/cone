[package]
name = "gl"
version = "0.1.0"
output = "library"

# OpenGL's import library, opengl32.lib, from the Windows SDK, which Visual
# Studio's environment already puts on LIB
[link]
libraries = ["opengl32"]
